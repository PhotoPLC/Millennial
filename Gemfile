source "https://rubygems.org"

# Esta gema incluye todo lo que GitHub Pages necesita (Jekyll y sus plugins)
gem "github-pages", group: :jekyll_plugins

# Opcional: Si usas Windows, añade esta línea para evitar errores de codificación
gem "tzinfo-data", platforms: [:mingw, :mswin, :x64_mingw, :jruby]
